#!/bin/bash -eu

cmd_path=`which nippo`
current_path=${cmd_path%nippo}

echo "nippo start"

echo "read config..."
config_text=`sed -e 's/:[^:\/\/]/="/g;s/$/"/g;s/ *=/=/g' ${current_path}nippo.conf`
config=(`echo ${config_text}`)
for c in ${config[@]}
do
  eval ${c}
done

